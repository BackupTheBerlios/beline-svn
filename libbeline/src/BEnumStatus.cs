using System;

namespace LibBeline {
  /// Status of run of a module
  public enum BEnumStatus {

    // Attributes
    Sleep = 1,
    ///
    Wait = 2,
    ///
    Running = 3,
    /// 
    Idle = 4
  } // enum BEnumType
} // namespace
